#include <iostream>
using namespace std;

int main(){
    int n = -65;

    // n>=0?cout<<"Number is Positive":cout<<"Number is Negative";

    cout << (n >= 0 ? "Positive" : "Negative") << endl;

    return 0;
}